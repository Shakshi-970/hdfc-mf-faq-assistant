# Mutual Fund FAQ Assistant — Python dependencies
# Tested on Python 3.11+

# ---------------------------------------------------------------------------
# Phase 3.1: Scheduler (GitHub Actions) + Scraping Service
# ---------------------------------------------------------------------------

# HTTP client — async, HTTP/2, used by scraper/fetcher.py
httpx[http2]==0.27.2

# HTML parsing — used by scraper/parser.py
beautifulsoup4==4.12.3
lxml==6.0.4

# Timezone support — used by scraper/run.py and ingestion/upsert.py
tzdata==2024.1

# ---------------------------------------------------------------------------
# Phase 3.2: Chunking + Embedding
# ---------------------------------------------------------------------------

# Tokeniser — cl100k_base, used for accurate token counting in chunker
tiktoken==0.12.0

# LangChain text splitter — RecursiveCharacterTextSplitter
langchain-text-splitters==0.3.11

# Embedding model — BAAI/bge-small-en-v1.5 (384-dim, local CPU)
sentence-transformers==5.4.1

# Vector store — Chroma Cloud (remote ChromaDB)
chromadb==1.5.7

# Numpy — required by sentence-transformers
numpy==2.4.4

# ---------------------------------------------------------------------------
# Phase 3.4: Query Pipeline
# ---------------------------------------------------------------------------

# LLM — Anthropic Claude API
anthropic==0.49.0

# REST API framework
fastapi==0.115.12

# ASGI server
uvicorn[standard]==0.34.0

# Request/response validation (FastAPI dependency)
pydantic==2.11.3

# ---------------------------------------------------------------------------
# Phase 3.5: Session Manager
# ---------------------------------------------------------------------------

# Redis client — used by RedisSessionBackend (prod only; optional for dev)
# Set REDIS_URL env var to activate; omit to use in-memory backend instead.
redis[hiredis]==5.2.1

# ---------------------------------------------------------------------------
# Phase 3.6: Chat UI
# ---------------------------------------------------------------------------

# Streamlit — web UI framework
streamlit==1.44.1

# HTTP client — used by Streamlit app to call the FastAPI backend
requests==2.32.3

# ---------------------------------------------------------------------------
# Phase 6: Groq LLM backend
# ---------------------------------------------------------------------------

# Groq Python SDK — used by GroqClient in phase_6_1_groq_pipeline/llm_client.py
groq==1.1.2

# ---------------------------------------------------------------------------
# Phase 7: Testing and Evaluation
# ---------------------------------------------------------------------------

# Test runner — used by phase_7_1_unit_tests/
pytest==8.3.5
